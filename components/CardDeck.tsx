// 카드 3개 + 셔플 버튼 (Zero-Scroll). TODO(Phase 1).
import type { Restaurant } from "@/types";

type Props = { cards: Restaurant[]; onShuffle: () => void };

export function CardDeck(_props: Props) {
  return <div>TODO: CardDeck</div>;
}
