// 식당 카드 1개 (상호·업종·도보시간·길찾기). TODO(Phase 1).
import type { Restaurant } from "@/types";

export function Card({ restaurant }: { restaurant: Restaurant }) {
  return <div>TODO: Card — {restaurant.name}</div>;
}
