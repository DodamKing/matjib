// 위치 동의 UI + geolocation 획득. TODO(Phase 1).
"use client";

type Props = { onLocate: (coords: { lat: number; lng: number }) => void };

export function LocationGate(_props: Props) {
  return <div>TODO: LocationGate</div>;
}
