// "AI 의사 처방" 카피 생성. 데이터 정확성과 분리된 브랜딩 레이어.
// TODO(Phase 2): 구현 (Claude 또는 템플릿).
import type { Restaurant } from "@/types";

/** 카드 묶음에 붙일 처방 카피 문구 생성. */
export function prescribe(_cards: Restaurant[], _query?: string): string {
  return "";
}
