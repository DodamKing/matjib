// 공정 셔플 + 후보 풀 관리 (DECISIONS D3). 품질 랭킹 아님 — 거리 적합 + 랜덤 추첨.
// TODO(Phase 1): 구현.
import type { Restaurant } from "@/types";

/** 후보 풀에서 공정하게 3개 추첨. */
export function pickThree(_pool: Restaurant[], _seed?: string): Restaurant[] {
  throw new Error("not implemented");
}
